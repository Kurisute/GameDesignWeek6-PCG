/// @description Insert description here
// You can write your code in this editor

if (bounce) {
	horiverti = irandom(1);
	if (horiverti) x += irandom(1) ? -32 : 32;
	else y += irandom(1) ? -32 : 32;
}